python --version
conda --version
pip --version
anaconda --version
conda env list
conda list
python -c "import ibapi"
echo "if desired, manual installation can be triggered: pip install -r requirements.txt --user"
echo "then run: cd /scripts/IBJts/source/pythonclient/ &&  python3 -m pip install ."